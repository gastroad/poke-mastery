import { PokemonInspector } from "@/client/dev/PokemonInspector";

/** Throwaway ideation surface: everything the app knows about one Pokémon.
    Not linked from anywhere; delete along with the component. */
export default function DevPokemonPage() {
  return <PokemonInspector />;
}
